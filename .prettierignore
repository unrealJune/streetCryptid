node_modules/
.expo/
dist/
web-build/
android/
ios/
coverage/
*.log
bun.lock
package-lock.json
yarn.lock
pnpm-lock.yaml
assets/
.env.example

# design archive (mockups + generated OSM data + renders) — reference only, not app code
docs/design/

# generated map tile fixtures (binary/base64 blobs)
src/features/map/tiles/__fixtures__/

# normative mesh wire vectors — emitted verbatim by `cargo test -- --ignored print_fixture`
# (tests/mesh_vectors.rs); reformatting it would make every regeneration a diff
modules/iroh-location/rust/tests/fixtures/mesh_vectors.json

# normative Double Ratchet schedule vectors — same deal (tests/ratchet_vectors.rs compares the
# file byte-for-byte against the emitted string, so prettier touching it fails the suite)
modules/iroh-location/rust/tests/fixtures/ratchet_vectors.json

# Gradle build output from the Wi-Fi Aware spike (untracked, regenerated on every build)
spike/*/*/build/
spike/*/build/

# wasm-pack generated glue and binary
modules/iroh-location/web/

# vendored/generated native sources use their own formatters
modules/iroh-location/rust/third_party/
modules/iroh-location/ios/generated/
modules/iroh-location/android/src/main/java/uniffi/
